# Change Log

All changes made to project files are recorded here.  
Format: `YYYY-MM-DD | file(s) | description`

---

<!-- Log entries go here -->

2026-07-29 | stores/index.html, DB: oc_information_description (information_id=8) | Embedded the standalone `/stores/` store-locator widget inside Information page 8 via a self-sizing iframe. Made stores/index.html mount-independent (relative `widget.css`/`widget.js` instead of hardcoded `/oc/stores/…`) and added a postMessage height reporter so the embedding iframe auto-resizes. Replaced the page-8 description hacks (`.content{display:block}` + polling) with a clean iframe + adaptive path (prod `/oc/stores/`, local `/stores/`) + height listener. NOTE: page-8 description lives in the DB (not rsync-deployed) — the same snippet must be pasted into the production admin editor.

2026-07-29 | catalog/model/journal3/filter.php, catalog/controller/journal3/product_extras.php | Product Label "latest" preset now labels products added within the last 1 month with no count limit (true "NEW" badge). Added opt-in `date_added_months` cutoff in filter.php `getProducts()` (only fires when the param is passed, so other filters are unaffected) and a dedicated `case 'latest'` in product_extras.php that sets `date_added_months=1` and `limit=0`. Cleared cached `journal3.module.product_label` so assignments rebuild. Local sanity check: 132 products in last month vs 5796 total enabled.

2026-08-01 | DB: oc_journal3_module (module_id=1155, product_label "Swiss") | Turned the unused/disabled "Swiss" product label into an image-only label showing `image/catalog/bandiera_svizzera.webp` on every product in categories 32 (Philip Watch), 60 (Philip Watch Γυναικεία) and 75 (Philip Watch Αντρικά) — 119 products. Settings: status=true, type=custom, filter preset=advanced with categories ["32","60","75"] and limit="" (0 = no cap), label text emptied for both languages, labelBackground = flag image (no-repeat / center / contain) with `overwrite=true` + transparent color to kill the default dark pill, labelPadding=0, labelSize=40x26px (100x64 image ratio), position kept "group" (top-right stack). Previous categories were 159/204/189, which no longer exist. Cleared `journal3.*` cache so assignments rebuild. NOTE: config lives in the DB — deploy with `scripts/journal-push.sh`; `/image/` is gitignored, so the .webp must also be uploaded to production separately.

2026-08-01 | PRODUCTION DB: oc_journal3_module (module_id=1155, product_label "Swiss") | Applied the same "Swiss" image label config directly to production (local push was not possible). Surgical update of row 1155 only — NOT a full `journal-push.sh`, so the rest of the production Journal config was left untouched. Production already had the identical disabled module and the image at image/catalog/bandiera_svizzera.webp. Row backed up before the update (copy kept in the session scratchpad); 103 journal3.* cache files cleared afterwards. Verified live: /oc/ categories 32, 60, 75 show 12/12 flags per page, category 49 shows 0, resized image https://timeonline.gr/oc/image/cache/catalog/bandiera_svizzera-100x64.webp returns 200. Helper scripts removed from the server afterwards.
